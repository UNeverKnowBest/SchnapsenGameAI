[package]
name = "schnapsen-engine"
version = "0.1.0"
edition = "2024"
description = "Deterministic Schnapsen simulator with a pinned Python differential oracle"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bench]]
name = "independent_games"
harness = false
